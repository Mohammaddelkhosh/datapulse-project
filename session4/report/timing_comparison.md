# مقایسه زمان اجرای کوئری‌ها (قبل و بعد از ایندکس)

| کوئری | زمان قبل از ایندکس | زمان بعد از ایندکس | بهبود |
|-------|-------------------|--------------------|-------|
| جستجوی مشتری (WHERE customer_id) | ... ms | ... ms | ... |
| گزارش ماهانه (GROUP BY order_date) | ... ms | ... ms | ... |
| تجمیع سفارشات مشتری | ... ms | ... ms | ... |

کوئری	زمان قبل از ایندکس	زمان بعد از ایندکس	بهبود / تحلیل
جستجوی مشتری (WHERE customer_id)	0.039 ms	0.042 ms	حجم دکم‌کمِ داده‌ها (Seq Scan ترجیحی)
گزارش ماهانه (GROUP BY order_date)	0.169 ms	0.073 ms	بهبود جزئی در زمانِ تجمیع و اجرا
تجمیع سفارشات مشتری	0.169 ms	0.073 ms	(مشابه کوئری قبل)


# مقایسه زمان اجرای کوئری‌ها (قبل و بعد از ایندکس)

| کوئری | زمان قبل از ایندکس | زمان بعد از ایندکس | بهبود / تحلیل |
|-------|-------------------|--------------------|---------------|
| جستجوی مشتری (WHERE customer_id) | 0.039 ms | 0.042 ms | حجم کم داده‌ها (Seq Scan ترجیحی توسط Optimizer) |
| گزارش ماهانه (GROUP BY order_date) | 0.169 ms | 0.073 ms | بهبود جزئی در زمانِ تجمیع و اجرا |
| تجمیع سفارشات مشتری | 0.169 ms | 0.073 ms | عملکرد مشابه به دلیل تعداد کم رکوردهای تست |
